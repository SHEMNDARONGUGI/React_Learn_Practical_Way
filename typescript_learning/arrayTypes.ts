const nums: number[] = [1, 2, 4, 6];
const str: string[] = ["one", "two", "three"];

console.log(nums, str);

//Adding Items to an array
const items: string[] = [];
items.push("Shem", "Steve", "Caleb");
console.log(items);

const numList: number[] = [];
numList.push(20);
console.log(numList);

// Making an ray in another way
const itemsArray: Array<string> = ["Shem", "Caleb", "Stever"];
console.log(itemsArray);
